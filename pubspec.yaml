name: khaja_byte_flutter
description: Khājā Byte — official canteen app of ING College of Innovation and Leadership.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  intl: ^0.19.0
  mobile_scanner: ^7.4.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
